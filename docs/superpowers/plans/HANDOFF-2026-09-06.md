# VHS Restore Studio — 引継書（Handoff）

最終更新: 2026-09-06（Task 11 完了、独立レビュー指摘を反映済み）

## 完了状態

- Task 1–11 完了。実装ブランチは `work/vhs-restore-studio`。
- 独立 reviewer の重要指摘（tasklist 失敗を成功扱いにしない、TFF/BFF を fps だけでスキップしない）を修正済み。
- テスト: 全件 pytest をこの作業ツリーで再実行し、**132 passed, 1 skipped**。
- GUI: `QT_QPA_PLATFORM=offscreen` で `MainWindow` 構築（title=VHS Restore Studio、プリセット 5）。
- GitHub リモートなし。PR は作成できない。ローカル `main` へマージする。

## 起動

```powershell
cd C:\Users\issho\vhs-restore-studio
.\setup.ps1    # 初回
.\run.ps1      # GUI（python -m vhs_restore / vhs-restore も可）
.\doctor.ps1   # 依存診断
```

## Task 11 内容

1. `tests/test_e2e_synthetic.py` + `tests/fixtures/media.py`（29.97i TFF/BFF、59.94p、日本語/空白パス、音声、cancel、DVD、preview==full、ソース 4:3 SAR）
2. Windows `taskkill` が missing と明示したときだけ PID なし扱い。`tasklist` 失敗は未確認としてエラー。
3. auto deinterlace: Progressive の高フレームレートはスキップ。Mixed は progressive メタデータがあるときだけスキップ。TFF/BFF は fps でスキップしない。
4. README と `task-9-report.md` / `task-10-report.md` / `task-11-report.md`

## Known Issues（残）

- `doctor.ps1` は ffmpeg `--version` が nonzero だと exit 1（実体は動く）
- vspipe / QTGMC 未導入 → bwdif
- video2x / realesrgan-ncnn-vulkan 未導入 → classical scaler
- Real-ESRGAN は動画パスに使わない
- リモート未設定のため GitHub PR 不可

## 品質方針

元映像に存在しないものを作らず、VHS の情報を自然に保持・復元する。4:3 を 16:9 に stretch しない。Preview と本処理は同一 `build_pipeline`。
