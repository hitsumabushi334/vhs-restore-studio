# VHS Restore Studio — 引継書（Handoff）

最終更新: 2026-09-06（Task 11 完了。独立 reviewer の P0/P1 を反映済み）

## 完了状態

- Task 1–11 完了。実装ブランチは `work/vhs-restore-studio`。
- 独立 reviewer を複数回実施。残っている指摘は P2（vspipe stderr を JobError に載せない）のみ。
- テスト: **141 passed, 1 skipped**（worktree で再実行）。
- GUI: `QT_QPA_PLATFORM=offscreen` で `MainWindow`（title=VHS Restore Studio、プリセット 5、`python -m vhs_restore` の `main` あり）。
- GitHub リモートなし。PR は作成できない。ローカル `main` へマージする。

## 起動

```powershell
cd C:\Users\issho\vhs-restore-studio
.\setup.ps1    # 初回
.\run.ps1      # GUI（python -m vhs_restore / vhs-restore も可）
.\doctor.ps1   # 依存診断
```

## Task 11 以降の修正（reviewer）

- cancel が process tree を止められなかった場合は `JobError`（成功した cancelled にしない）。`_cancel_finished` で競合を同期。
- QTGMC 選択時は `vspipe --y4m script.vpy -` を FFmpeg `pipe:0` にバイナリ接続。音声は元ソースを第二入力 `-map 0:v:0 -map 1:a?`。
- プレビュー trim は pipe に `-ss` しない。映像は `trim` フィルタ、音声は入力 `-ss`。
- 生成スクリプトは `import vapoursynth as vs` から始まる。ソースと同パスの .vpy は上書きしない。

## Known Issues（残）

- `doctor.ps1` は ffmpeg `--version` が nonzero だと exit 1（実体は動く）
- vspipe / QTGMC 未導入 → bwdif
- video2x / realesrgan-ncnn-vulkan 未導入 → classical scaler
- Real-ESRGAN は動画パスに使わない
- リモート未設定のため GitHub PR 不可
- QTGMC 失敗時に vspipe stderr を JobError へ十分載せない（P2）

## 品質方針

元映像に存在しないものを作らず、VHS の情報を自然に保持・復元する。4:3 を 16:9 に stretch しない。Preview と本処理は同一 `build_pipeline`。
