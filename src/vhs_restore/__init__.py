"""VHS Restore Studio package scaffold."""

__version__ = "0.1.0"


def main() -> int:
    """Run the package entry point until the GUI is added in a later task."""
    print(f"VHS Restore Studio {__version__} scaffold ready")
    return 0
